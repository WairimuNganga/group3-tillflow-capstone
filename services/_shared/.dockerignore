# Keep the build context to what the image actually needs — smaller context,
# faster builds, and nothing local/dev-only can leak into a layer.
.venv/
**/__pycache__/
**/*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
tests/
local/
.git/
.gitignore
.DS_Store
