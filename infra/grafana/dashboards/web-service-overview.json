{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "id": null,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "AMP" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(web_requests_total[5m]))",
          "refId": "A"
        }
      ],
      "title": "Request rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "AMP" },
      "fieldConfig": { "defaults": { "unit": "percentunit", "max": 1, "min": 0 }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(web_requests_total{status=\"5xx\"}[5m])) / clamp_min(sum(rate(web_requests_total[5m])), 1e-9)",
          "refId": "A"
        }
      ],
      "title": "5xx ratio",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["tillflow", "web"],
  "title": "Platform / web — Service overview",
  "uid": "tillflow-web-overview",
  "version": 1
}
